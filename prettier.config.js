/** @type {import('prettier').Config & import('prettier-plugin-tailwindcss').PluginOptions} */
const config = {
  trailingComma: 'all',
  semi: false,
  arrowParens: 'avoid',
  singleQuote: true,
  tabWidth: 2,
}

export default config
